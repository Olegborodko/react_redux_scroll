import React from "react";
import TextField from '@mui/material/TextField';

export const Input = () => {
  return (
    <TextField
      style={{ paddingTop: "10px" }}
      defaultValue=""
      size="small"
    />
  )
}